{
  "name": "dor-lol",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel serve -d dist src/index.html",
    "build": "rm -rf build && parcel build -d build --public-url /DOR-LOL-1T/ src/index.html",
    "deploy": "gh-pages -d build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^6.1.1"
  },
  "dependencies": {
    "parcel-bundler": "^1.12.5"
  }
}
